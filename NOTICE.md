# Clean-Room Boundary

This repository must not contain KDE Connect source code, copied snippets, generated files,
or Git history.

The separate clone at `/Users/jianruicheng/GitHub/kdeconnect-kde-upstream` is only for
reading concepts such as envelope shape, transport abstraction, pairing state, input event
categories, and clipboard loop suppression.

Any implementation copied from KDE Connect would bring GPL obligations into this repository,
which is explicitly out of scope for EdgeLink.
